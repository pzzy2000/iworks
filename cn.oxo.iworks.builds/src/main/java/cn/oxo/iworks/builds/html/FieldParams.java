package cn.oxo.iworks.builds.html;

public class FieldParams {

      public enum FieldType {
            text, passwd, localCombo, noType, RemoteCombo, Email, Date, Money, MoneyShow, Rate, RateShow, Read, textarea
      }

      public enum FieldShow {
            Input, Search, Grid
      }

}
